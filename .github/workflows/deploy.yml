name: Deploy to Azure

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Provision infra and deploy app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy infrastructure (Bicep)
        id: infra
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            outputs=$(az deployment sub create \
              --name "taskmgr-$(date +%Y%m%d%H%M%S)" \
              --location westeurope \
              --template-file infra/main.bicep \
              --parameters infra/main.bicepparam \
              --query properties.outputs -o json)
            echo "swaName=$(echo "$outputs" | jq -r '.staticWebAppName.value')" >> "$GITHUB_OUTPUT"
            echo "rgName=$(echo "$outputs" | jq -r '.resourceGroupName.value')" >> "$GITHUB_OUTPUT"
            echo "swaHost=$(echo "$outputs" | jq -r '.staticWebAppHostname.value')" >> "$GITHUB_OUTPUT"

      - name: Get Static Web App deployment token
        id: token
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            token=$(az staticwebapp secrets list \
              --name "${{ steps.infra.outputs.swaName }}" \
              --resource-group "${{ steps.infra.outputs.rgName }}" \
              --query "properties.apiKey" -o tsv)
            echo "::add-mask::$token"
            echo "token=$token" >> "$GITHUB_OUTPUT"

      - name: Deploy front end + API to Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.token.outputs.token }}
          action: "upload"
          app_location: "frontend"
          api_location: "src/TaskManager.Api"
          output_location: ""

      - name: Summary
        run: |
          echo "### Deployed :rocket:" >> "$GITHUB_STEP_SUMMARY"
          echo "- Resource group: \`${{ steps.infra.outputs.rgName }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Static Web App: \`${{ steps.infra.outputs.swaName }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- URL: https://${{ steps.infra.outputs.swaHost }}" >> "$GITHUB_STEP_SUMMARY"
